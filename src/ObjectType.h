#pragma once
enum ObjectType {
	PLAYER,
	PLATFORM,
	HAZARD,
	BACKGROUND,
	MIDGROUND,
	FOREGROUND,
	START_MENU,
	PAUSE_MENU,
	OVER_MENU,
};

enum ButtonType {
	START,
	RESUME,
	RESTART,
	QUIT
};